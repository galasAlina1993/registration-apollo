module.exports = {
    id: ({ id }) => id,
    name: ({ name }) => name,
    email: ({ email }) => email,
    phone: ({ phone }) => phone,
    address: ({ address }) => address,
    zipCode: ({ zipCode }) => zipCode,
    files: ({ files }) => files
}